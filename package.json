{
  "name": "felixthedevgit-profile",
  "version": "1.0.0",
  "private": true,
  "description": "Profile page with self-drawn graphics, no third-party services",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "build:offline": "node scripts/build.js --offline"
  }
}
